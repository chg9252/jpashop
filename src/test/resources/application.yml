spring:
#  datasource:
#    url: jdbc:h2:mem:test  # 전부다 주석처리해버리면 스프링 부트는 기본적으로 MemoryDB를 사용한다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop // 자원정리를 하기위해 drop까지 한다.
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # hibernate에서 생성하는 모든 sql이 debug에서 보임
    org.hibernate.type: trace # 파라미터에 어떤값이 들어갔는지 확인할 수 있다.
